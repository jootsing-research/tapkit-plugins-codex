[[screens]]
id = "welcome"

[[screens.modes]]
id = "normal"
description = "The first-launch welcome sheet introduces Reminders features above a single Continue button."

[[screens.modes.elements]]
position = [500, 892]
description = "continue button — advance through the UI-only welcome flow"
target = "lists.icloud-prompt"

[[screens]]
id = "overview"

[[screens.modes]]
id = "normal"
description = "The Lists overview combines search, smart-list tiles, an iCloud syncing suggestion, the local Reminders list, and creation controls."

[[screens.modes.elements]]
position = [920, 66]
description = "edit button — enter list reordering and management mode"
target = "overview.edit"

[[screens.modes.elements]]
position = [450, 106]
description = "search field — focus reminder search"
target = "overview.search"

[[screens.modes.elements]]
position = [916, 106]
description = "dictation button — start voice input for search"
looksMutating = true

[[screens.modes.elements]]
position = [259, 191]
description = "today tile — show reminders due today"
target = "today.normal"

[[screens.modes.elements]]
position = [760, 191]
description = "scheduled tile — show reminders with scheduled dates"
target = "scheduled.normal"

[[screens.modes.elements]]
position = [259, 287]
description = "all tile — show all reminders"
target = "all.normal"

[[screens.modes.elements]]
position = [760, 287]
description = "completed tile — show completed reminders"
target = "completed.normal"

[[screens.modes.elements]]
position = [908, 363]
description = "dismiss iCloud suggestion button — hide the syncing suggestion"
looksMutating = true

[[screens.modes.elements]]
position = [307, 463]
description = "go to settings link — leave Reminders for iCloud settings without changing anything"

[[screens.modes.elements]]
position = "any list row under My Lists"
isRepeating = true
description = "list row — open that reminder list"
target = "lists.normal"

[[screens.modes.elements]]
position = [203, 756]
description = "new reminder button — begin creating a reminder"
looksMutating = true

[[screens.modes.elements]]
position = [879, 756]
description = "add list button — begin creating a list"
looksMutating = true

[[screens.modes]]
id = "edit"
description = "Lists editing mode shows visibility toggles and reorder handles for smart lists, plus deletion, information, and reorder controls for local lists."

[[screens.modes.elements]]
position = [920, 66]
description = "done button — leave Lists editing mode"
target = "overview.normal"

[[screens.modes.elements]]
position = "any smart-list visibility checkmark"
isRepeating = true
description = "smart-list visibility checkmark — show or hide that smart list"
looksMutating = true

[[screens.modes.elements]]
position = "any smart-list reorder handle"
isRepeating = true
description = "smart-list reorder handle — rearrange the smart lists"
looksMutating = true

[[screens.modes.elements]]
position = "the minus button beside a local list"
description = "remove list button — begin deleting the local list"
looksMutating = true

[[screens.modes.elements]]
position = "the information button beside a local list"
description = "list information button — open the list appearance editor"
target = "list-info.normal"

[[screens.modes.elements]]
position = "the reorder handle beside a local list"
description = "list reorder handle — rearrange the local list"
looksMutating = true

[[screens.modes.elements]]
position = [879, 756]
description = "add list button — begin creating a list"
looksMutating = true

[[screens.modes]]
id = "search"
description = "Search mode focuses the search field over a dimmed Lists overview and adds a Cancel button."

[[screens.modes.elements]]
position = [430, 70]
description = "search field — enter a transient reminder query"

[[screens.modes.elements]]
position = [752, 70]
description = "dictation button — start voice input for search"
looksMutating = true

[[screens.modes.elements]]
position = [891, 70]
description = "cancel button — leave search without a query"
target = "overview.normal"

[[screens]]
id = "lists"

[[screens.modes]]
id = "icloud-prompt"
description = "The empty built-in Reminders list is dimmed beneath an alert asking whether to enable iCloud syncing in Settings."

[[screens.modes.elements]]
position = [91, 66]
description = "back button — return to the presenting screen"

[[screens.modes.elements]]
position = [927, 66]
description = "more button — open list-management options"

[[screens.modes.elements]]
position = [327, 469]
description = "not now button — dismiss the syncing suggestion without changing settings"
target = "lists.normal"

[[screens.modes.elements]]
position = [668, 469]
description = "go to settings button — leave Reminders for its system settings without changing anything"

[[screens.modes.elements]]
position = [203, 756]
description = "new reminder button — begin creating a reminder"
looksMutating = true

[[screens.modes]]
id = "normal"
description = "The empty built-in Reminders list shows no reminder rows, with back navigation, list options, and a New Reminder action."

[[screens.modes.elements]]
position = [91, 66]
description = "back button — return to the Lists overview"

[[screens.modes.elements]]
position = [927, 66]
description = "more button — open list-management options"
target = "lists.menu"

[[screens.modes.elements]]
position = [203, 756]
description = "new reminder button — begin creating a reminder"
looksMutating = true

[[screens.modes]]
id = "menu"
description = "A list-management popover overlays the empty list with information, selection, sorting, visibility, printing, and deletion actions."

[[screens.modes.elements]]
position = [620, 109]
description = "show list info row — open unsaved name, icon, and color controls"
target = "list-info.normal"

[[screens.modes.elements]]
position = [620, 151]
description = "select reminders row — enter multi-selection mode"
target = "lists.select"

[[screens.modes.elements]]
position = [620, 201]
description = "sort by row — open sort criteria options"
target = "lists.sort-menu"

[[screens.modes.elements]]
position = [620, 255]
description = "show completed row — change whether completed reminders are visible"
looksMutating = true

[[screens.modes.elements]]
position = [620, 297]
description = "print row — open the system print configuration"
target = "print-options.normal"

[[screens.modes.elements]]
position = [620, 339]
description = "delete list button — permanently delete this list after confirmation"
looksMutating = true

[[screens.modes]]
id = "sort-menu"
description = "The expanded Sort By submenu offers manual, due-date, creation-date, priority, and title ordering."

[[screens.modes.elements]]
position = [620, 179]
description = "sort by row — collapse sort criteria options"
target = "lists.menu"

[[screens.modes.elements]]
position = "any sort criterion row"
isRepeating = true
description = "sort criterion row — change the list ordering preference"
looksMutating = true

[[screens.modes]]
id = "select"
description = "Selection mode for the empty list changes the title and exposes disabled bulk date, move, delete, and more actions."

[[screens.modes.elements]]
position = [898, 66]
description = "done button — leave reminder selection mode"
target = "lists.normal"

[[screens.modes.elements]]
position = "any disabled bulk action along the bottom"
isRepeating = true
description = "bulk action — change or delete selected reminders"
looksMutating = true

[[screens]]
id = "list-info"

[[screens.modes]]
id = "normal"
description = "List Info presents unsaved appearance controls for the Reminders list, including its icon, name, and color palette."

[[screens.modes.elements]]
position = [107, 82]
description = "cancel button — discard unsaved list appearance changes and return"

[[screens.modes.elements]]
position = [898, 82]
description = "done button — save list appearance changes"
looksMutating = true

[[screens.modes.elements]]
position = [500, 181]
description = "current list icon preview — no action"

[[screens.modes.elements]]
position = [500, 274]
description = "list name field — edit the unsaved list name"
looksMutating = true

[[screens.modes.elements]]
position = "any color swatch in the palette"
isRepeating = true
description = "color swatch — choose an unsaved list color"

[[screens]]
id = "today"

[[screens.modes]]
id = "normal"
description = "Today groups the empty due-today view into Morning, Afternoon, and Tonight sections."

[[screens.modes.elements]]
position = [91, 66]
description = "back button — return to the Lists overview"

[[screens.modes.elements]]
position = [927, 66]
description = "more button — open Today view options"
target = "today.menu"

[[screens.modes.elements]]
position = [203, 756]
description = "new reminder button — begin creating a reminder due today"
looksMutating = true

[[screens.modes]]
id = "menu"
description = "Today view options provide selection, time grouping, sorting, and printing."

[[screens.modes.elements]]
position = [620, 109]
description = "select reminders row — enter selection mode"
target = "today.select"

[[screens.modes.elements]]
position = [620, 151]
description = "group by time row — change whether today is split into time-of-day sections"
looksMutating = true

[[screens.modes.elements]]
position = [620, 201]
description = "sort by row — open Today sort criteria"
target = "today.sort-menu"

[[screens.modes.elements]]
position = [620, 255]
description = "print row — open the system print configuration"
target = "print-options.normal"

[[screens.modes]]
id = "sort-menu"
description = "The expanded Today Sort By submenu offers due-date and other ordering criteria."

[[screens.modes.elements]]
position = [620, 179]
description = "sort by row — collapse Today sort criteria"
target = "today.menu"

[[screens.modes.elements]]
position = "any sort criterion row"
isRepeating = true
description = "sort criterion row — change Today ordering"
looksMutating = true

[[screens.modes]]
id = "select"
description = "Today selection mode shows no reminder rows and disabled bulk actions."

[[screens.modes.elements]]
position = [898, 66]
description = "done button — leave reminder selection mode"
target = "today.normal"

[[screens.modes.elements]]
position = "any disabled bulk action along the bottom"
isRepeating = true
description = "bulk action — change or delete selected reminders"
looksMutating = true

[[screens]]
id = "scheduled"

[[screens.modes]]
id = "normal"
description = "Scheduled groups the empty view under Today, upcoming dates, later months, and future years."

[[screens.modes.elements]]
position = [91, 66]
description = "back button — return to the Lists overview"

[[screens.modes.elements]]
position = [927, 66]
description = "more button — open Scheduled view options"
target = "scheduled.menu"

[[screens.modes.elements]]
position = "the chronological scheduled-reminder content"
description = "scheduled content — scroll through future date groups"

[[screens.modes]]
id = "menu"
description = "Scheduled view options provide selection, completed-item visibility, and printing."

[[screens.modes.elements]]
position = [620, 109]
description = "select reminders row — enter selection mode"
target = "scheduled.select"

[[screens.modes.elements]]
position = [620, 151]
description = "show completed row — change whether completed scheduled reminders are visible"
looksMutating = true

[[screens.modes.elements]]
position = [620, 201]
description = "print row — open the system print configuration"
target = "print-options.normal"

[[screens.modes]]
id = "select"
description = "Scheduled selection mode exposes disabled bulk operations because there are no reminder rows."

[[screens.modes.elements]]
position = [898, 66]
description = "done button — leave reminder selection mode"
target = "scheduled.normal"

[[screens.modes.elements]]
position = "any disabled bulk action along the bottom"
isRepeating = true
description = "bulk action — change or delete selected reminders"
looksMutating = true

[[screens]]
id = "all"

[[screens.modes]]
id = "normal"
description = "All groups every active reminder by source list; the local Reminders group is currently empty."

[[screens.modes.elements]]
position = [91, 66]
description = "back button — return to the Lists overview"

[[screens.modes.elements]]
position = [927, 66]
description = "more button — open All view options"
target = "all.menu"

[[screens.modes]]
id = "menu"
description = "All view options provide selection, completed-item visibility, and printing."

[[screens.modes.elements]]
position = [620, 109]
description = "select reminders row — enter selection mode"
target = "all.select"

[[screens.modes.elements]]
position = [620, 151]
description = "show completed row — change whether completed reminders are visible"
looksMutating = true

[[screens.modes.elements]]
position = [620, 201]
description = "print row — open the system print configuration"
target = "print-options.normal"

[[screens.modes]]
id = "select"
description = "All selection mode exposes disabled bulk operations because there are no reminder rows."

[[screens.modes.elements]]
position = [898, 66]
description = "done button — leave reminder selection mode"
target = "all.normal"

[[screens.modes.elements]]
position = "any disabled bulk action along the bottom"
isRepeating = true
description = "bulk action — change or delete selected reminders"
looksMutating = true

[[screens]]
id = "completed"

[[screens.modes]]
id = "normal"
description = "Completed shows the completed reminder count and an empty completed-reminder area."

[[screens.modes.elements]]
position = [91, 66]
description = "back button — return to the Lists overview"

[[screens.modes.elements]]
position = [927, 66]
description = "more button — open Completed view options"
target = "completed.menu"

[[screens.modes.elements]]
position = [385, 157]
description = "clear button — permanently clear completed reminders"
looksMutating = true

[[screens.modes]]
id = "menu"
description = "Completed view options provide selection and printing."

[[screens.modes.elements]]
position = [620, 109]
description = "select reminders row — enter selection mode"
target = "completed.select"

[[screens.modes.elements]]
position = [620, 151]
description = "print row — open the system print configuration"
target = "print-options.normal"

[[screens.modes]]
id = "select"
description = "Completed selection mode exposes disabled bulk operations because there are no completed reminder rows."

[[screens.modes.elements]]
position = [898, 66]
description = "done button — leave reminder selection mode"
target = "completed.normal"

[[screens.modes.elements]]
position = "any disabled bulk action along the bottom"
isRepeating = true
description = "bulk action — change or delete selected reminders"
looksMutating = true

[[screens]]
id = "print-options"

[[screens.modes]]
id = "normal"
description = "The system print sheet previews the empty list and offers printer, copies, paper, orientation, scaling, layout, sharing, and printing controls."

[[screens.modes.elements]]
position = [107, 82]
description = "cancel button — dismiss printing and return to the list"

[[screens.modes.elements]]
position = [781, 82]
description = "share button — open destinations for exporting the print document"
looksMutating = true

[[screens.modes.elements]]
position = [908, 82]
description = "print button — submit the configured print job"
looksMutating = true

[[screens.modes.elements]]
position = [500, 147]
description = "printer row — choose a printer for the prospective job"
looksMutating = true

[[screens.modes.elements]]
position = "copies minus or plus button"
isRepeating = true
description = "copies stepper — change the prospective copy count"
looksMutating = true

[[screens.modes.elements]]
position = [500, 264]
description = "paper size row — choose paper size for the prospective job"
looksMutating = true

[[screens.modes.elements]]
position = "portrait or landscape orientation button"
isRepeating = true
description = "orientation button — change the prospective page orientation"
looksMutating = true

[[screens.modes.elements]]
position = "scaling minus or plus button"
isRepeating = true
description = "scaling stepper — change the prospective print scale"
looksMutating = true

[[screens.modes.elements]]
position = [500, 435]
description = "layout row — choose pages per sheet for the prospective job"
looksMutating = true
