[[screens]]
id = "notifications-onboarding"

[[screens.modes]]
id = "location-alert"
description = "The Maps notifications onboarding page is dimmed behind a system alert asking to turn on Location Services for Maps."

[[screens.modes.elements]]
position = [312, 573]
description = "settings button — would leave Maps to change the persistent Location Services setting"
looksMutating = true

[[screens.modes.elements]]
position = [697, 573]
description = "cancel button — dismiss the Location Services alert without changing authorization"
target = "notifications-onboarding.normal"

[[screens.modes]]
id = "normal"
description = "The Maps onboarding page explains that notifications provide turn-by-turn directions, shared ETAs, and report updates."

[[screens.modes.elements]]
position = [500, 881]
description = "enable notifications button — would request or change persistent notification authorization"
looksMutating = true

[[screens.modes.elements]]
position = [500, 941]
description = "not now button — continue without changing notification authorization"
target = "ads-onboarding.normal"

[[screens]]
id = "ads-onboarding"

[[screens.modes]]
id = "normal"
description = "A privacy notice explains that Maps may show local ads based on approximate location, searches, or the visible map, without linking advertising information to the Apple Account."

[[screens.modes.elements]]
position = [500, 886]
description = "about Apple Advertising and Privacy link — attempt to open Apple's explanatory privacy page; the onboarding page remained visible on this phone"

[[screens.modes.elements]]
position = [500, 912]
description = "continue button — acknowledge the advertising privacy notice and continue"
target = "map.normal"

[[screens]]
id = "map"

[[screens.modes]]
id = "normal"
description = "The main Maps globe view with Location Services off and a partially expanded bottom sheet containing search, account access, saved Places, and Guides."

[[screens.modes.elements]]
position = [500, 61]
description = "Location Services is Off banner — would offer a path to change persistent Location Services authorization"
looksMutating = true

[[screens.modes.elements]]
position = [500, 330]
description = "map canvas — pan the globe without changing user data"

[[screens.modes.elements]]
position = [897, 462]
description = "map options button — open map type and display controls"
target = "map.map-modes"

[[screens.modes.elements]]
position = [897, 521]
description = "current location button — ask Maps to locate the device; blocked because Location Services is off"
looksMutating = true

[[screens.modes.elements]]
position = [500, 571]
description = "sheet handle — drag to resize the Maps bottom sheet"

[[screens.modes.elements]]
position = [286, 606]
description = "search field — enter destination and place queries"
target = "map-search.normal"

[[screens.modes.elements]]
position = [752, 606]
description = "voice search button — would request microphone authorization and begin voice input"
looksMutating = true

[[screens.modes.elements]]
position = [889, 606]
description = "account button — open account, favorites, guides, and Maps settings"
target = "map.account"

[[screens.modes.elements]]
position = [248, 673]
description = "Places disclosure — expand or collapse saved place shortcuts"

[[screens.modes.elements]]
position = [162, 752]
description = "add Home button — would save a Home address"
looksMutating = true

[[screens.modes.elements]]
position = [387, 752]
description = "add Work button — would save a Work address"
looksMutating = true

[[screens.modes.elements]]
position = [613, 752]
description = "add place button — would add a saved place"
looksMutating = true

[[screens.modes.elements]]
position = [320, 886]
description = "Your Guides disclosure — open the guides collection"

[[screens.modes.elements]]
position = "any guide card in the horizontal carousel; moves as the sheet scrolls"
isRepeating = true
description = "guide card — open a curated guide"

[[screens.modes]]
id = "map-modes"
description = "A Map Modes sheet overlays the globe and offers Explore, Driving, Transit, and Satellite presentation choices; the selected choice has a blue outline."

[[screens.modes.elements]]
position = [885, 738]
description = "close button — dismiss Map Modes and return to the main map"
target = "map.normal"

[[screens.modes.elements]]
position = [162, 818]
description = "Explore map mode button — show the standard detailed map"

[[screens.modes.elements]]
position = [389, 818]
description = "Driving map mode button — emphasize road conditions and driving information"

[[screens.modes.elements]]
position = [611, 818]
description = "Transit map mode button — emphasize public-transit routes"

[[screens.modes.elements]]
position = [825, 818]
description = "Satellite map mode button — show satellite imagery"
target = "map.satellite-modes"

[[screens.modes]]
id = "satellite-modes"
description = "The Map Modes sheet with Satellite selected expands to include Traffic and Labels display toggles; these persistent presentation settings are left unchanged."

[[screens.modes.elements]]
position = [885, 605]
description = "close button — dismiss Map Modes and return to the main satellite map"
target = "map.normal"

[[screens.modes.elements]]
position = [162, 676]
description = "Explore map mode button — return to the standard detailed map"
target = "map.map-modes"

[[screens.modes.elements]]
position = [389, 676]
description = "Driving map mode button — emphasize road conditions and driving information"
target = "map.map-modes"

[[screens.modes.elements]]
position = [611, 676]
description = "Transit map mode button — emphasize public-transit routes"
target = "map.map-modes"

[[screens.modes.elements]]
position = [825, 676]
description = "Satellite map mode button — keep satellite imagery selected"

[[screens.modes.elements]]
position = [827, 818]
description = "Traffic toggle — would change the persistent satellite traffic overlay setting"
looksMutating = true

[[screens.modes.elements]]
position = [827, 877]
description = "Labels toggle — would change the persistent satellite labels setting"
looksMutating = true

[[screens.modes]]
id = "account"
description = "An account sheet overlays the map with sign-in, Places, Reports, Offline Maps, and Preferences rows."

[[screens.modes.elements]]
position = [885, 482]
description = "close button — dismiss the account sheet"
target = "map.normal"

[[screens.modes.elements]]
position = [500, 646]
description = "Apple Account sign-in button — would begin authentication and change account state"
looksMutating = true

[[screens.modes.elements]]
position = [513, 729]
description = "Places row — open saved places"
target = "places.normal"

[[screens.modes.elements]]
position = [513, 788]
description = "Reports row — open submitted Maps reports"
target = "reports.normal"

[[screens.modes.elements]]
position = [513, 847]
description = "Offline Maps row — open downloaded-map management"
target = "offline-maps.normal"

[[screens.modes.elements]]
position = [513, 906]
description = "Preferences row — open Maps navigation and route preferences"
target = "preferences.normal"

[[screens]]
id = "places"

[[screens.modes]]
id = "normal"
description = "The Places sheet summarizes Pinned, Saved Places, Guides, and Routes, all empty on this phone, with an empty Recently Added section."

[[screens.modes.elements]]
position = [897, 150]
description = "close button — dismiss Places and return to the map"
target = "map.normal"

[[screens.modes.elements]]
position = [513, 221]
description = "Pinned row — open pinned places"
target = "pinned.normal"

[[screens.modes.elements]]
position = [513, 284]
description = "Saved Places row — open saved places"
target = "saved-places.normal"

[[screens.modes.elements]]
position = [513, 346]
description = "Guides row — open personal guides"
target = "guides.normal"

[[screens.modes.elements]]
position = [513, 407]
description = "Routes row — open saved routes"
target = "routes.normal"

[[screens]]
id = "pinned"

[[screens.modes]]
id = "normal"
description = "The Pinned sheet offers empty Home and Work shortcuts plus a Siri suggestion for Nearby Transit."

[[screens.modes.elements]]
position = [897, 150]
description = "close button — dismiss Pinned and return to the map"
target = "map.normal"

[[screens.modes.elements]]
position = [300, 225]
description = "add Home row — would save a Home address"
looksMutating = true

[[screens.modes.elements]]
position = [300, 300]
description = "add Work row — would save a Work address"
looksMutating = true

[[screens.modes.elements]]
position = [450, 478]
description = "Nearby Transit suggestion row — unexpectedly promoted the suggestion into Pinned when tapped; avoid during safe mapping"
looksMutating = true
target = "pinned.nearby-transit-pinned"

[[screens.modes.elements]]
position = [880, 478]
description = "pin Nearby Transit button — would add the suggestion to Pinned"
looksMutating = true

[[screens.modes.elements]]
position = [430, 950]
description = "add pinned place button — would create a new pinned place"
looksMutating = true

[[screens.modes.elements]]
position = [570, 950]
description = "edit pinned places button — enter editing; used only to undo an unintended Nearby Transit pin"
target = "pinned.edit"

[[screens.modes]]
id = "edit"
description = "Pinned editing mode shows removal controls for Home, Work, and the accidentally pinned Nearby Transit item, reorder handles, and a completion checkmark."

[[screens.modes.elements]]
position = [110, 225]
description = "remove Home button — would remove the Home shortcut"
looksMutating = true

[[screens.modes.elements]]
position = [110, 300]
description = "remove Work button — would remove the Work shortcut"
looksMutating = true

[[screens.modes.elements]]
position = [110, 407]
description = "remove Nearby Transit button — remove the accidentally pinned suggestion to restore the initial state"
looksMutating = true

[[screens.modes.elements]]
position = [880, 407]
description = "Nearby Transit reorder handle — would change pinned-item order"
looksMutating = true

[[screens.modes.elements]]
position = [870, 950]
description = "done button — commit the restoration and exit editing"
looksMutating = true

[[screens.modes]]
id = "nearby-transit-pinned"
description = "Pinned after the Nearby Transit suggestion was unexpectedly promoted into the main list; this transient state is immediately reverted."

[[screens.modes.elements]]
position = [897, 150]
description = "close button — dismiss Pinned and return to the map"
target = "map.normal"

[[screens.modes.elements]]
position = [300, 225]
description = "add Home row — would save a Home address"
looksMutating = true

[[screens.modes.elements]]
position = [300, 300]
description = "add Work row — would save a Work address"
looksMutating = true

[[screens.modes.elements]]
position = [450, 388]
description = "Nearby Transit pinned row — open nearby departure options"

[[screens.modes.elements]]
position = [430, 950]
description = "add pinned place button — would create a new pinned place"
looksMutating = true

[[screens.modes.elements]]
position = [570, 950]
description = "edit pinned places button — enter editing to undo the unintended pin"
target = "pinned.edit"

[[screens]]
id = "map-search"

[[screens.modes]]
id = "normal"
description = "The expanded Maps search sheet has the keyboard focused, a natural-language search tip, and nearby category shortcuts."

[[screens.modes.elements]]
position = [400, 127]
description = "focused search field — type a destination or place query"
target = "map-search.query-location-alert"

[[screens.modes.elements]]
position = [755, 127]
description = "voice search button — would request microphone authorization and begin voice input"
looksMutating = true

[[screens.modes.elements]]
position = [900, 127]
description = "close button — cancel search and return to the main map"
target = "map.normal"

[[screens.modes.elements]]
position = [880, 257]
description = "dismiss search tip button — would persistently dismiss the natural-language search tip"
looksMutating = true

[[screens.modes.elements]]
position = "any Find Nearby category row, such as Gas Stations or Breakfast; moves as the sheet scrolls"
isRepeating = true
description = "nearby category row — run a category search; not exercised because Location Services is off"

[[screens.modes]]
id = "query-location-alert"
description = "A live text query has produced suggestions, but a Location Services system alert blocks the search sheet because location is off."

[[screens.modes.elements]]
position = [312, 573]
description = "settings button — would leave Maps to change persistent Location Services authorization"
looksMutating = true

[[screens.modes.elements]]
position = [697, 573]
description = "cancel button — dismiss the location alert without changing authorization"
target = "map-search.query"

[[screens.modes]]
id = "query"
description = "The focused search sheet contains a partial query and live suggestions while Location Services remains off."

[[screens.modes.elements]]
position = [400, 127]
description = "query field — edit the current search text"

[[screens.modes.elements]]
position = [755, 127]
description = "clear query button — clear the current search text"
target = "map-search.normal"

[[screens.modes.elements]]
position = [900, 127]
description = "close button — cancel search and return to the main map"
target = "map.normal"

[[screens.modes.elements]]
position = "any live search suggestion row; moves with results"
isRepeating = true
description = "search suggestion row — open a matching place or region; not exercised because location is unavailable"

[[screens.modes.elements]]
position = [870, 712]
description = "keyboard search button — submit the rendered query; not used because live suggestions already triggered the location alert"

[[screens]]
id = "saved-places"

[[screens.modes]]
id = "normal"
description = "An empty Saved Places sheet invites the user to add a place."

[[screens.modes.elements]]
position = [897, 150]
description = "close button — dismiss Saved Places and return to Places"

[[screens.modes.elements]]
position = [500, 653]
description = "add a place button — would create a saved place"
looksMutating = true

[[screens]]
id = "guides"

[[screens.modes]]
id = "normal"
description = "The Guides sheet contains an empty Favorites guide and controls to create or select guides."

[[screens.modes.elements]]
position = [897, 150]
description = "close button — dismiss Guides and return to Places"

[[screens.modes.elements]]
position = [500, 195]
description = "Favorites guide row — open the Favorites guide"
target = "favorite-guide.normal"

[[screens.modes.elements]]
position = [430, 950]
description = "add guide button — would create a new guide"
looksMutating = true

[[screens.modes.elements]]
position = [570, 950]
description = "select guides button — enter guide selection mode"
target = "guides.select"

[[screens.modes]]
id = "select"
description = "Guide selection mode for choosing guides to edit or delete; Favorites is the only guide and no selection is made."

[[screens.modes.elements]]
position = [500, 195]
description = "Favorites guide selection row — tapping the protected Favorites guide did not select it on this phone"

[[screens.modes.elements]]
position = [130, 950]
description = "delete selected guides button — disabled until a guide is selected; would delete selected guides"
looksMutating = true

[[screens.modes.elements]]
position = [870, 950]
description = "done button — exit guide selection mode"
target = "guides.normal"

[[screens]]
id = "favorite-guide"

[[screens.modes]]
id = "normal"
description = "The empty Favorites guide appears as a bottom sheet over the globe and invites adding places."

[[screens.modes.elements]]
position = [885, 604]
description = "close button — dismiss the Favorites guide"

[[screens.modes.elements]]
position = [500, 571]
description = "sheet handle — drag to resize the Favorites guide sheet"

[[screens.modes.elements]]
position = [855, 940]
description = "add place button — would add a place to Favorites"
looksMutating = true

[[screens]]
id = "routes"

[[screens.modes]]
id = "normal"
description = "An empty Routes sheet explains custom walking routes and offers route creation."

[[screens.modes.elements]]
position = [897, 150]
description = "close button — dismiss Routes and return to Places"

[[screens.modes.elements]]
position = [500, 687]
description = "create route button — would begin creating and saving a custom route"
looksMutating = true

[[screens]]
id = "reports"

[[screens.modes]]
id = "normal"
description = "An empty Reports sheet explains that submitted street, place, and route issues would appear here."

[[screens.modes.elements]]
position = [897, 150]
description = "close button — dismiss Reports and return to the account sheet"

[[screens.modes.elements]]
position = [500, 950]
description = "report a new issue button — would begin submitting a Maps report"
looksMutating = true

[[screens]]
id = "offline-maps"

[[screens.modes]]
id = "normal"
description = "An empty Offline Maps sheet explains offline routing and offers downloading a new map."

[[screens.modes.elements]]
position = [897, 91]
description = "close button — dismiss Offline Maps and return to the account sheet"

[[screens.modes.elements]]
position = [500, 350]
description = "download new map button — would choose and download an offline region"
looksMutating = true

[[screens]]
id = "preferences"

[[screens.modes]]
id = "normal"
description = "Maps Preferences with Driving selected and persistent route-avoidance settings for driving, walking, and cycling; settings are recorded but not changed."

[[screens.modes.elements]]
position = [897, 150]
description = "close button — dismiss Preferences and return to the account sheet"

[[screens.modes.elements]]
position = [500, 261]
description = "Driving directions preference — currently selected; would change the persistent preferred travel mode"
looksMutating = true

[[screens.modes.elements]]
position = [500, 310]
description = "Walking directions preference — would change the persistent preferred travel mode"
looksMutating = true

[[screens.modes.elements]]
position = [500, 363]
description = "Transit directions preference — would change the persistent preferred travel mode"
looksMutating = true

[[screens.modes.elements]]
position = [500, 417]
description = "Cycling directions preference — would change the persistent preferred travel mode"
looksMutating = true

[[screens.modes.elements]]
position = [840, 529]
description = "Avoid Tolls toggle — would change a persistent driving preference"
looksMutating = true

[[screens.modes.elements]]
position = [840, 588]
description = "Avoid Highways toggle — would change a persistent driving preference"
looksMutating = true

[[screens.modes.elements]]
position = [840, 707]
description = "walking Avoid Hills toggle — would change a persistent walking preference"
looksMutating = true

[[screens.modes.elements]]
position = [840, 766]
description = "Avoid Busy Roads toggle — would change a persistent walking preference"
looksMutating = true

[[screens.modes.elements]]
position = [840, 829]
description = "Avoid Stairs toggle — would change a persistent walking preference"
looksMutating = true

[[screens.modes.elements]]
position = [840, 944]
description = "cycling Avoid Hills toggle — would change a persistent cycling preference"
looksMutating = true

[[screens.modes.elements]]
position = [500, 760]
description = "preferences list — scroll to additional settings without changing them"
target = "preferences.scrolled"

[[screens.modes]]
id = "scrolled"
description = "Preferences scrolled to show cycling Avoid Busy Roads and persistent transit-mode choices in addition to the route-avoidance toggles."

[[screens.modes.elements]]
position = [897, 150]
description = "close button — dismiss Preferences and return to the account sheet"

[[screens.modes.elements]]
position = [840, 194]
description = "Avoid Tolls toggle — would change a persistent driving preference"
looksMutating = true

[[screens.modes.elements]]
position = [840, 257]
description = "Avoid Highways toggle — would change a persistent driving preference"
looksMutating = true

[[screens.modes.elements]]
position = [840, 375]
description = "walking Avoid Hills toggle — would change a persistent walking preference"
looksMutating = true

[[screens.modes.elements]]
position = [840, 435]
description = "Avoid Busy Roads toggle — would change a persistent walking preference"
looksMutating = true

[[screens.modes.elements]]
position = [840, 498]
description = "Avoid Stairs toggle — would change a persistent walking preference"
looksMutating = true

[[screens.modes.elements]]
position = [840, 608]
description = "cycling Avoid Hills toggle — would change a persistent cycling preference"
looksMutating = true

[[screens.modes.elements]]
position = [840, 671]
description = "cycling Avoid Busy Roads toggle — would change a persistent cycling preference"
looksMutating = true

[[screens.modes.elements]]
position = [500, 786]
description = "Bus transit preference — currently enabled; would change persistent transit-mode filtering"
looksMutating = true

[[screens.modes.elements]]
position = [500, 837]
description = "Subway and Light Rail transit preference — currently enabled; would change persistent transit-mode filtering"
looksMutating = true

[[screens.modes.elements]]
position = [500, 891]
description = "Commuter Rail transit preference — currently enabled; would change persistent transit-mode filtering"
looksMutating = true

[[screens.modes.elements]]
position = [500, 944]
description = "Ferry transit preference — currently enabled; would change persistent transit-mode filtering"
looksMutating = true
